tasks:
  - id: 1
    title: "期限管理コンポーネント作成"
    description: |
      期限管理UIを実装する。

      作成するファイル:
      1. src/components/DueDatePicker.tsx
         - 日付選択UI（input type="date" または カスタムピッカー）
         - 選択した日付を親コンポーネントに渡す
         - クリアボタン付き

      2. src/components/DueDateDisplay.tsx
         - 期限日の表示
         - 期限切れ: 赤色で「期限切れ」表示
         - 期限間近（3日以内）: オレンジ色で警告
         - 通常: グレーで日付表示

      技術要件:
      - TypeScript + React
      - Tailwind CSS でスタイリング
      - 既存の Todo 型を参照（src/types/todo.ts）

      出力先: /mnt/c/Users/shige/antigravity/nepolabo-todo/src/components/
    target_path: "/mnt/c/Users/shige/antigravity/nepolabo-todo/src/components/"
    priority: high
    status: assigned
    assigned_at: "2026-01-29T21:59:29"
